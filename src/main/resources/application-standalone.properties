server.address=127.0.0.1
# No database in standalone: without this the datasource auto-config fails the context on a
# missing JDBC url just because the driver is on the classpath for the full profile.
spring.autoconfigure.exclude=org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration
