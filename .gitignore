# Session-local working papers — not part of the deliverable
HANDOFF.md

# Local agent state
.claude/settings.local.json

# Build output
target/

# Source briefs held for reference only. They are the assessor's material, not
# ours to publish — this repository is public. Kept on disk, never committed.
docs/source/

# Tooling caches
.pytest_cache/
.serena/
__pycache__/

# Keys never land by accident. These two are deliberate, test-only and labelled in
# src/test/resources/README; anything else has to be un-ignored on purpose too.
*.pem
!src/test/resources/test-jwt-private.pem
!src/test/resources/test-jwt-public.pem
