# OWASP ZAP baseline — per-rule dispositions. TAB-separated: <rule id> <IGNORE|WARN|FAIL> <comment>
#
# THIS FILE IS THE ONLY PLACE A FINDING IS TOLERATED, and that is deliberate. `ci.yml` sets
# `fail_action: true`, so the job goes red on anything the scan reports — which means every accepted
# finding has to be written down here, with its rule id and a reason, where it shows up in a diff.
# The alternative the action offers by default (`fail_action: false`) is a scan that reports findings
# and exits 0: the same defect as the Sonar step that submitted an analysis and never read the
# verdict, and as the deleted stage 6.
#
# IGNORE means "this does not apply to this system", never "we could not be bothered". If a rule
# genuinely applies, fix it in `docker/traefik/dynamic.yml` — headers belong on the terminator —
# rather than adding a line here.
#
# ── Observed report, run 31257068048, against https://127.0.0.1:8443 ────────────────────────────
# NOTE: that run predates the move to the privileged pair. The job now targets `https://127.0.0.1`
# (port 443). The counts below have not been re-measured at the new target — the entry they justify
# is about a response body rather than an origin, so it is expected to hold, but treat the tally as
# the record of a superseded configuration until a run at 443 replaces it.
#   FAIL-NEW: 0   WARN-NEW: 1   PASS: 66
# At that run the TLS work's headers all passed on their own merits and needed no entry:
# Strict-Transport-Security, X-Content-Type-Options, X-Frame-Options, Content Security Policy,
# Permissions Policy, Cross-Domain Misconfiguration, Private IP Disclosure and Application Error
# Disclosure.
#
# STRICT-TRANSPORT-SECURITY HAS SINCE BEEN TURNED OFF DELIBERATELY, so rule 10035 is expected to
# report it missing from the next run onward and is dispositioned below. That is a real reduction in
# what this gate asserts, and it is written down rather than absorbed.
#
# ── A LIMIT OF THIS SCAN, STATED RATHER THAN LEFT TO BE DISCOVERED ──────────────────────────────
# The same run reported:
#   Job spider error accessing URL https://127.0.0.1:8443 status code returned : 401 expected 200
# That is correct behaviour and it is also the ceiling on what this gate can see. A baseline scan
# spiders for links and then reads passively; this is a bearer-token JSON API with no anonymous
# surface and no hyperlinks, so the spider found exactly two URLs and both answered 401. **This job
# checks the edge — TLS, redirect, headers, error shapes — and does NOT scan the API's routes.**
#
# The upgrade is `zap-api-scan.py` fed `docs/api/openapi.yaml`, which enumerates routes from the
# contract instead of crawling for them. It is not wired, and would need a token minted for it.
# Adding authentication to *this* baseline would not help: with or without a token the spider still
# has no links to follow.

10049	IGNORE	Non-Storable Content, on two 401 responses. The rule flags content a cache cannot store, which is a caching observation rather than a weakness — and on an authenticated ledger API non-storable is the REQUIRED behaviour, not a defect. Making this rule pass would mean making 401s cacheable.

10035	IGNORE	Strict-Transport-Security Header Not Set. The header is deliberately NOT sent, and this is the one entry here that tolerates a genuine weakness rather than a non-applicable rule. The application's router is a catch-all, so a browser dialling https://localhost would receive the pin -- and HSTS is host-scoped and PORT-INDEPENDENT, so a one-year pin on the bare host `localhost` force-upgrades http://localhost:3000, http://localhost:8080 and every other local development server on that machine, failing with ERR_SSL_PROTOCOL_ERROR and clearable only through chrome://net-internals/#hsts. Breaking a developer's unrelated projects is too high a price for a header on a throwaway certificate for a hostname nobody outside this machine resolves. A REAL DEPLOYMENT ON A REAL DOMAIN MUST SET IT, with includeSubDomains, and this line must not travel with it -- docs/urls-and-tls.md and docs/pitfalls.md both record that as a named gap.
